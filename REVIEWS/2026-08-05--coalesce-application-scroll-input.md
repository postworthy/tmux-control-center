# Review Record: Coalesce Application Scroll Input

Date: 2026-08-05
Review Boundary: merge from `fix/c014-app-scroll-burst` into `main`
Merge Method: `git merge --no-ff fix/c014-app-scroll-burst`
Risk Class: T1
Related Proposal: `PROPOSALS/2026-08-05--coalesce-application-scroll-input.md`
Related RCA: `RCA/2026-08-05--application-scroll-input-burst-disconnect.md`

## Decision

Pending physical regression acceptance. Canonical verification, production
build, and local diff review pass. No merge, deployment, push, or publication is
authorized by this record; the owner separately authorized the test deployment
on 2026-08-05.

## Scope and Compatibility

- [x] One synthetic gesture buffers negotiated xterm reports and sends once.
- [x] Maximum 72-report order and bytes are preserved below the client envelope.
- [x] The buffer resets before and after every gesture and empty input sends no
  serialized message.
- [x] Velocity bands, event cap, Older/Latest, modifiers, typing, paste, default
  history, server limiter defaults, schemas, and backend behavior are unchanged.
- [x] Canonical verification and production image pass.
- [ ] Repeated physical maximum gestures produce no disconnect or new limiter
  warning.

## Evidence

- Live pre-fix logs: three consecutive terminal connections close immediately
  after `Terminal input rate limit exceeded` on the affected session.
- Source boundary: maximum 72 per-report sends exceeded the default 64-message
  token bucket; byte volume was not limiting.
- Frontend unit tests: pass. A fixture concatenates 72 distinct worst-coordinate
  SGR reports, preserves exact order, and produces one serialized message below
  12,000 bytes.
- Frontend typecheck: pass.
- Canonical `./scripts/verify.sh`: pass with 24 Core, 12 Infrastructure (four
  isolated tests skipped), 33 Server integration, all three frontend suites,
  and Compose validation; server burst rejection remains green.
- Production image `sha256:6ac99e...`: pass with only the current application
  bundles and worker cache `tmux-mobile-shell-62ae5cab06ed4b01`; C013 recency and
  App Scroll markers remain present.
- Owner-approved tailnet test deployment: pass. C013 is preserved as
  `tmux-mobile:pre-c014-scroll-rollback`; live image `sha256:6ac99e...` is healthy
  on the unchanged exact-IP bind. HTTPS liveness/root, readiness, current
  root/terminal/worker identity, compatibility across 13 tmux sessions, and
  direct-backend 426 checks pass. No post-restart rate-limit warning is present
  before the physical maximum-gesture test.

## Risk and Rollback

- Coalescing reuses the existing serializer and xterm-generated bytes; it neither
  invents escape sequences nor weakens server protection.
- Rollback is a revert of C014 or restoration of the preserved pre-C014 image;
  no data or configuration migration is involved.

## Findings

- Blocking: physical regression acceptance.
- Non-blocking: none recorded.
